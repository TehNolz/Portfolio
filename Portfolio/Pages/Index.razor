@page "/"
@using Markdig
@using System.Reflection
@using System.IO

<h1>Hello, world!</h1>
@foreach (var manifestResourceName in Assembly.GetExecutingAssembly().GetManifestResourceNames())
{
    <p>@manifestResourceName</p>
}